Save these files into a folder in an easily accessable location. 
Open ruby on rails console. 
Navigate to the new directory. 
Type "rails server -b $IP -p $PORT" into your the console to open the server.
Open localhost:3000/ on an internet browser.